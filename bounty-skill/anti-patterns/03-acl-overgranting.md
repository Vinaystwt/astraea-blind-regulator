# ACL Overgranting

Do not grant individual result handles to everyone, the issuer, or the regulator by default.

Grant only:

- contract access for future computation/decryption flow
- investor access for their own result
- regulator access for aggregate report handles
